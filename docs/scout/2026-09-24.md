# Task scout — 2026-09-24

Scout: Bramble.
Ref audited: `scout/2026-09-24` @ `908f3da2` == `origin/next` (`git rev-parse --abbrev-ref HEAD` → `scout/2026-09-24`; `git rev-parse --short HEAD` → `908f3da2`). Every file:line below was read from that tree.
Rooms read: Roomy Space `#bugs` (`01KWF70N615JGPF5EG0HFE7DHP`, last 100 → 2026-08-14T19:14…2026-09-18T03:28Z — **newest message unchanged since the 2026-09-18 scout run**), Muni Town `#todos` (`01KWF3WPGPSWBN69RPGGGVVF0S`, last 100 → 2026-07-24T09:08…2026-09-16T01:55Z), Meri's Forest `#meta` (`01M278XWXG60HET50VJYVP01YX`, last 60 → **covers its full history**, 2026-09-11T03:42…08:53Z), Meri's Forest `#coordination` (`01M0HME2E3FEYRG72X2YWQRHDZ`, last 40 → 2026-09-22T08:36…2026-09-24T17:19Z), Muni Town `#roomy` (`01KWF2RMWHD5D6B02PM3SBTMQA`, last 40 → 2026-09-22T11:39…2026-09-24T16:38Z), Muni Town `#features` (`01KWF70JH1TTRNV3ANZSXDVR90`, last 25 → 2026-09-03…2026-09-22T14:27Z), Roomy Space `#general` (`01KWF70D0M0ZENVQWMYMDSWFZV`, last 25 → 2026-09-12T23:53…2026-09-24T17:26Z), Meri's Forest `#bramble` (`01M2EX4PQYW94DBCX6PJVDDS7X`, last 6).
Logs: Loki `appserver` (1,208 error entries / 7d — **604 `scope=xrpc` + 604 `scope=app`**, one stack echo per request), `discord-bridge` (320 / 7d), `roomy-app-lite` (1,402 / 7d), plus 24h scope/level aggregates; Mimir `roomy_*` (22 series); Tempo `appserver` (3h); live probes of `/health`, `/health/embed`, `/metrics`, `roomy.space/build.json`; the fork's PR list and push-event feed via the integration API.
Queue snapshot: 20 open tasks.

## Ref/live state — the appserver is current, the bridge is not, and one merge is recorded that no ref carries

- **Appserver:** `/health` → `build_id 908f3da2`, which **is this fork's `origin/next` tip** (verified after a fresh `git fetch origin next`). Unlike yesterday, there is no undeployed appserver merge.
- **app-lite:** `roomy.space/build.json` → `824daab9`, built 2026-09-23T07:35:34Z. Three commits are newer, but `git diff --stat 824daab9..origin/next -- packages/app-lite packages/design` is **empty** — nothing user-facing landed in those commits, so app-lite is content-current.
- **discord-bridge:** the running process reports `build_id d84e2a4d` on every line since the 17:53Z sample, and the 7d query for `msg="bridge starting"` returns **exactly one** match: `2026-09-23T05:19:59.391Z d84e2a4d`. The bridge image was republished for `908f3da2` (workflow run 35968868168, 07:18:01→07:20:01Z) but the service has not restarted, so **the fix in proposal 2 is merged and not running.**
- **`next` was rewritten twice yesterday morning, and one recorded merge is not on any ref.** The fork's push-event feed for `refs/heads/next` on 2026-09-24 reads: `2d5051dc → ab9a9904` (06:47:59Z), `ab9a9904 → 6eb8d435` (06:48:49Z), `6eb8d435 → 908f3da2` (06:54:55Z), `908f3da2 → 758ef1b2` (07:15:23Z), **`758ef1b2 → 908f3da2` (07:17:59Z)**. The PR API's `merge_commit_sha` for `#252`, `#256` and `#261` is `ab9a9904`, `6eb8d435` and `758ef1b2` respectively; after a full `git fetch origin` (546 refs) **none of the three objects exists locally**, while the *content* of #252/#256 lives on as `d24cb28f`/`908f3da2`. `#261`'s content does not live on anywhere (proposal 1).
- **`#coordination` is where the loop's own answers go, and the 09-24 ticks are clean.** The 13:16Z and 17:19Z reports both read `read-coord.sh` → 1 message, no Meri instruction in window; 9/9 digests `ok` except a resolved `ALERT:pr-issues(1)` for #262.

## Queue audit

- **TASK-196's fix is merged but not running, and its remedy is recorded merged but absent.** `#256`'s dedupe is on `origin/next`; the bridge process is `d84e2a4d`. Verified in code, not inferred: `git show d84e2a4d:packages/discord-bridge/src/services/room-sync.ts` has **0** occurrences of `toLowerCase` in the merge, `git show origin/next:…/room-sync.ts` has **3** (case-insensitive `byName` keying plus lowercased lookups at `:453`, `:500`, `:509`). `#261` — the remediation tool Meri said on 2026-09-23T06:48Z she was "looking into a way to recover" for — is `merged: true` with `merged_at 2026-09-24T07:15:22Z` and is on no ref. Proposals 1 and 2.
- **STALE TASK-189 — the defect no longer reproduces, and its fix is still unmerged.** `GET /xrpc/space.roomy.message.getMessage failed: Entity … is not a message` is 145 lines / 7d, but the **newest is 2026-09-23T04:52:43Z** — 0 in the ~37 h since, on `908f3da2`, which does not carry `#249`. The task's acceptance ("zero … over 24h on a build carrying the change") is met by the *current* build. Re-measure before it is closed either way.
- **STALE — 20 open PRs, 15 of them not scout reports.** `#229` (TASK-159's P0 fix, created 2026-09-18T08:21Z, **6 days**), `#234` (TASK-169, 09-18T20:17Z), `#227` (TASK-138 plan, **09-16T15:30Z, 8 days**), `#246`, `#249`, `#253`–`#260`, `#262`, `#263`, plus five scout PRs (`#217`, `#228`, `#237`, `#245`, `#251`). Every one reports `mergeable: true` / `mergeable_state: clean`. This is the sixth consecutive day the scout has reported an unreviewed backlog.
- **TASK-21 ~ TASK-138 — sixth flag, still open.** Flagged 09-16, 09-18, 09-21, 09-22, 09-23. Neither is dispatchable as written. Not actioned here (no unilateral closes, per the TASK-20 precedent).
- **BLOCKED TASK-63 — sixth flag, 27 days at P1.** The plan has awaited the same five decisions since 2026-08-28. New input, not a re-argument: the demand is now visible to users — Mr Blizzard asked in Muni Town `#roomy` at 2026-09-24T15:59Z *"Wait does roomy have voice chat?"*, answered "No, it doesn't yet" by Zicklag.
- **CORRECTION, third pass — TASK-166(b)'s headline claim still does not reproduce, and its recorded evidence is still the wrong test.** Re-measured today against the built artefacts with the symbols' actual export paths: `stableKeyId` appears **0× in the root `dist/index.js` and 4× in `dist/browser/index.js`**, and `packages/sdk/src/browser/index.ts` re-exports only `createTanstackCacheAdapter` from `./tanstack` — so `stableKeyId` is reachable through *no* subpath, which makes the original "absent from dist, present in src" comparison true by construction. `deleteMessages` appears **1× in root `dist/index.js`, 0× in `dist/browser/index.js`**, matching `src/operations`. The per-subpath drift test the 09-23 scout ran (0 src-exports missing on `.`, `./browser`, `./package/operations`) therefore stands: **there is no measurement under this entry's operational question** (fold a rebuild into the next rollout?). It remains a decision owed to Meri, on nothing measured.
- **No CONFLICT pairs; no open task whose prose cites its own shipped work as unbuilt.** `#meta` has **no new messages** — its full history is 13 entries, all 2026-09-11 — so the self-improvement quota has no fresh source today; the closest new signal is Meri's `#coordination` direction (2026-09-23), which is hers to dispatch.

## Proposals (ranked)

### 1. PR #261 is recorded `merged` and its code is on no ref — and it is the only remedy for the duplicate-category regression that is still live

- **Evidence.** `GET …/pulls/261` → `{"state":"closed","merged":true,"merged_at":"2026-09-24T07:15:22Z","merge_commit_sha":"758ef1b26277748300bdd8a6bbe8a53442ea0cc4","head_sha":"9aea6fbc…","changed_files":4,"additions":1104}`. After a full `git fetch origin`: `git cat-file -t 758ef1b2` → *Not a valid object name*; `git ls-remote origin | grep 758ef1b2` → **not found among 546 refs**; `git fetch origin 758ef1b2` → *couldn't find remote ref*. The push-event feed shows `refs/heads/next` moving `908f3da2 → 758ef1b2` at **07:15:23Z** and back to `908f3da2` at **07:17:59Z** — a rollback ~2.5 minutes after the merge.
  The content check is independent of the SHA: the PR's four files are `packages/discord-bridge/src/scripts/{recover-sidebar,sidebar-recovery}.ts`, `…/__tests__/sidebar-recovery.test.ts` and a README section. All three sources are **absent** from `origin/next`'s tree, `git grep "recover-sidebar\|sidebar-recovery" origin/next -- packages/` returns **no match**, the classifier's distinctive vocabulary (`multi-sync`, `pre-sync`, `no-sync-event`) appears in **4 files on the PR head and 0 on `next`**, and the README's recovery section is 2 matches on the head, 0 on `next`.
  The branch itself still exists: `refs/heads/fix/task-196-sidebar-recovery` @ `9aea6fbc`.
- **Why now.** Meri's stated intent (Muni Town `#roomy`, 2026-09-23T06:48Z) was *"Looking into a way to recover from it"*, and the affected population is every bridged space — a real user, Katzedecimal, confirmed on **2026-09-24T14:02Z**: *"Yeah i see that in my server."* With this tool off `next`, there is no remedy in the tree, while the merge record tells any reader that there is.
- **Scope sketch.** Reconcile the record, not the code: either re-land `9aea6fbc` onto `next` (it is a standalone ops script; `#256` already covers the forward fix and the two touched disjoint files), or record the rollback explicitly on TASK-196 and close the PR as not-merged. Whichever happens, the audit consequence is proposal 4.
- **Acceptance.** Either `git grep -l "sidebar-recovery" origin/next` matches, or `pulls/261` no longer reports `merged: true`; and TASK-196's entry names which of the two is true.
- **Suggested priority:** **P1** — a user-visible regression with a single remedy that the record says shipped.
- **Not already tracked because.** TASK-196 is recorded `done` against `#256`; nothing in the queue mentions `#261`, the rollback, or the recovery tool.
- **Honest limit.** I cannot tell *why* it was rolled back — no reviewer identity, no comment (the issue-events feed for #261 carries only `merged` then `closed` at 07:15:22Z). "Deliberate revert" and "accident" are both consistent with what I can measure.

### 2. The dedupe fix for TASK-196 is merged and the bridge is still executing the buggy merge — the duplication is live for every bridged space

- **Evidence.** Running process: every `discord-bridge` line sampled at 17:52Z–17:53Z carries `build_id d84e2a4d`; the 7d query for `msg="bridge starting"` returns **exactly one** line, `2026-09-23T05:19:59.391Z`, on `d84e2a4d`. The fix that addresses the duplication (`#256`, `908f3da2`) is on `next`, and the image built from it was published successfully (workflow run 35968868168, 07:18:01Z) — but the service is a manual redeploy, so nothing restarted.
  The regression's mechanism is confirmed in the running commit's source, not inferred: `git show d84e2a4d:…/room-sync.ts:440` builds `byName` from `cat.name` **verbatim** and `:474` looks up `byName.get(category.name)`, with the only dedupe being a per-category `present.has(roomId)`; `origin/next:…/room-sync.ts` lowercases both (`:453`, `:500`, `:509`). Ten guilds were synced into that code path on 2026-09-23 (`Initial structure synced …` ×10, 05:24:51–05:25:06Z, plus one at 15:33:24Z).
- **Why now.** The duplication has been user-visible since 09-23 and is user-confirmed on 09-24 (Katzedecimal). Re-landing the recovery tool (proposal 1) is pointless until the forward fix is actually running, and every further restart of the *old* image re-applies the bad merge path to any guild not yet marked.
- **Scope sketch.** No code: redeploy the discord-bridge service on the already-published `next` image, and record the bridge's deploy trigger in the README so "merged" vs "running" is checkable rather than remembered. (The image workflow already fires on `next` since `#243`; the gap is the service-side rollout, which is manual.)
- **Acceptance.** A `bridge starting` line carrying a build id ≥ `908f3da2`, and the affected spaces render one set of categories.
- **Suggested priority:** **P1**.
- **Not already tracked because.** TASK-181 ("the newest discord-bridge fixes are merged and cannot reach the bridge") was about the *image* never being published from `next`, and `#243` fixed that. This is the next hop: the image exists and the process did not move. No entry covers it.

### 3. The embed sweeper's stall diagnostic repeats ~2,100×/day for one unchanging cause, because a 396×/day gauge flap resets its own suppression latch

- **Evidence.** `scope="embed-sweeper"` emits **nothing but this one message, at `level=warn`**: `backlog stalled: oldest pending row is NNNNNm old and the last cycle selected nothing — cause=all-parked (pendingRows=816N selectableRows=0 parkedRows=816N backoffUrls=743N selected=0)`. Its 24h volume, measured four ways, is **2,081 / 2,086 / 2,530 / 2,640 lines** (instant `count_over_time`, `sum by(scope)`, and a 5,000-line range fetch, within ~25% of each other by window boundary — the 2,640 sample had **2,640 distinct timestamps**, no duplicates). Its per-hour histogram explains the spread: **544 / 809 / 327 lines in the three hours from 2026-09-23T17:00Z**, then a steady **6–45/h** (25 in the last hour). For scale, the same window holds **18** `level=warn` lines in `scope=materialize` and **1** in `push-deliver`; `embed-sweeper` is the appserver's largest warn source by two orders of magnitude.
  The code's own gate says this should not happen: `sweeper.ts` computes `const prevCause = backlogStuck ? stallCause : null` and logs only `if (prevCause !== cause)`, with the comment *"stay quiet on repeats so a 30s idle poll doesn't flood the log"*. Because `backlogStuck` is cleared on any cycle that recovers (`sweeper.ts:668-669`), a **1→0→1 flap resets `prevCause` to `null`**, and `null !== "all-parked"` re-logs. **[INFERENCE, read from the code]** — and it is the same 396/day flap the gauge shows, so log rate and transition rate agree (≈16/h expected vs 25 measured in the last hour).
  The gauge `roomy_embed_backlog_stuck` (60s steps, 24h): **957 samples `1`, 484 samples `0`, 396 transitions, 397 runs, 196 of them shorter than 30 minutes.** `/health/embed` at the same moment: `pending 8167, selectableRows 0, parkedRows 8167, backoffUrls 7439, inFlight 0, enrichedOk 68, enrichedNull 44610, backlogStuck true, lastStallCause "all-parked"`. Inter-line gaps: min 0.246s, median 4.5s, mean **32.7s** (≈ `IDLE_POLL_MS`).
  The first spike dates the stall: `roomy_embed_pending` was 5,116 at 09-23T15:02Z, **6,006 at 16:02Z, 7,441 at 17:02Z**, 7,611 at 18:02Z — a ~2,900-rows/hour flood that parked every URL at once and never drained.
- **Why now.** TASK-186/`#248` introduced this line and this gauge precisely so an operator could see the stall from Grafana. A gauge that toggles ~400×/day and a log line that repeats ~2,100×/day for an unchanged cause are the same "signal nobody can act on" shape TASK-142 names, and the log volume exceeds every other warning in the service by two orders of magnitude.
- **Scope sketch.** Make the suppression hold: latch on `cause` (log once per cause *transition*, independent of the `backlogStuck` flag's lifetime), or rate-limit the line to one per N minutes while stalled, and add a `backlogStuckTransitions` counter so the flapping is visible rather than inferred. Do **not** change the stall detection itself — `classifyPendingLinks`/`classifyStallCause` are measuring correctly (`selectableRows=0` matches `findPendingLinks`' skip set exactly).
- **Acceptance.** Over a 24h window in which `lastStallCause` does not change, `embed-sweeper` warn lines fall to single digits (baseline today: 2,081) and `roomy_embed_backlog_stuck` transitions fewer than 5 times (baseline today: 396); a new `backlogStuckTransitions` counter exposes the flap directly instead of requiring a 1,441-sample range query to see it.
- **Suggested priority:** **P3** (observability hygiene, no user impact).
- **Not already tracked because.** TASK-186 is done and its acceptance (report a *measured* cause instead of a fixed string) is met — the cause genuinely is `all-parked`. Nothing covers the rate of the report or the gauge's stability.
- **Honest limit.** I measured the volume and the flapping; I could **not** reproduce the suppression failing (no shell on the host), so *why* `prevCause !== cause` fails to hold is **[INFERENCE]**. The volume and the 0.13% success rate (`enrichedOk` 57 / null 44,596 since the restart) are measured.

### 4. `merge_commit_sha` is not a durable identifier here: three of the last five merges to `next` name a commit that is on no ref

- **Evidence.** The five most recent merges to `next` and the SHA the PR API recorded for each: `#247` → `d84e2a4d` (**on `next`**), `#248` → `2d5051dc` (**on `next`**), `#252` → `ab9a9904`, `#256` → `6eb8d435`, `#261` → `758ef1b2` — the last three **resolve to no object at all** after a full `git fetch origin` (`git cat-file -e <sha>` fails; `git ls-remote origin | grep <sha>` matches none of 546 refs). Their *content* survives only where the same change was re-committed: `#252`'s as `d24cb28f` and `#256`'s as `908f3da2`, both on `next`; `#261`'s nowhere (proposal 1). The push feed explains it: `refs/heads/next` moved `2d5051dc → ab9a9904 → 6eb8d435 → 908f3da2` inside seven minutes (06:47:59–06:54:55Z) — the two merges were re-committed under new SHAs — and separately `908f3da2 → 758ef1b2 → 908f3da2` at 07:15:23/07:17:59Z.
  This also explains a stale-looking observation in the 09-23 scout report: it recorded `824daab9` as existing "only on `muni-town/roomy`" — today `824daab9` is a direct parent of this fork's `next` tip, so upstream's commit is in the fork and the earlier read was a snapshot of a mid-flight refs state.
- **Why now.** `tasks-audit.sh` and `deploy-audit.sh` both decide "this shipped" from a merge — the scout brief's own definition is *"a PR the proxy reports MERGED, or an 8-hex commit that is an ancestor of origin/main"*. Each of those two arms is unsound against a rewritten `next`: the first says #261 shipped, the second cannot resolve the SHAs the first hands it. TASK-164 is the same class one artifact out (a claim whose truth is only checkable by cross-referencing another source), and this is its second live instance.
- **Scope sketch.** Change the predicate from *SHA reachability* to *content presence* — for a PR reported merged, assert that its head SHA's changed-file set (or a distinctive symbol from its diff) exists on the shipped ref, and emit a token when it does not; and have the digest report the reachability of each merge SHA it records, so "merged" and "on `next`" stop being treated as the same fact. Optionally, stop rewriting `next` (the rewrites are what make the SHA arm worthless).
- **Acceptance.** Running the new check against today's list flags `#261` (and, before the re-commit, would have flagged `#252`/`#256`), and is silent for `#247`/`#242`/`#241`, whose merge SHAs *are* ancestors of `next`.
- **Suggested priority:** **P2**.
- **Not already tracked because.** TASK-167/TASK-185 fixed *which* merges are read (title vs. branch name); neither asks whether the merge's SHA or content survives on the ref. No entry mentions that `next` is rewritten.

### 5. The open fix for the embed churn (`#258`) cannot fire in the state production is actually in

- **Evidence.** `#258` (TASK-197, open, `mergeable: clean`) adds `sweepYieldsAfter(result) => result.full && !result.producedOk`, where `full` is `pending.length >= SWEEP_BATCH` (25) and `producedOk` counts `ok` outcomes. Its own measured justification is a backlog that is *selectable* and churning: *"a full batch that produced no ok is churn"*.
  Production is in the opposite state. `/health/embed` right now: `pendingRows 8166, selectableRows 0, parkedRows 8166, backoffUrls 7438, inFlight 0`, `lastStallCause "all-parked"`. With every row's URL in the skip set, `findPendingLinks` (`enricher.ts:158-181`, `where url not in (…7438 placeholders…)`) returns **0** rows, so `pending.length = 0`, so `full = false`, so `sweepYieldsAfter` returns **false** and the loop takes `await waitForWake(IDLE_POLL_MS)` — the branch `#258` does not touch. The 2,640 stall lines/day of proposal 3 come from exactly this path.
  Where there *is* churn, the numbers still say `#258`'s half matters: `enrichedNull` climbed 29,637 → 44,596 between 09:00Z and 17:00Z today (**+14,959**), and it does so in bursts — hourly deltas of **+5,077 (10:00Z→11:00Z)** and **+5,123 (16:00Z→17:00Z)** against near-flat hours in between (**+9, +21, +14, +19** from 11:00Z to 15:00Z) — with `enrichedOk` 57 lifetime. The backlog also is not shrinking: `roomy_embed_pending` went 5,085 → 8,166 (+61%) between 2026-09-22T00:02Z and now, and `roomy_embed_transient_backoff` is 7,438 URLs against 8,166 rows.
- **Why now.** `#258` is one of the ten green PRs awaiting review, and its stated acceptance (a bounded cycle rate, a readable success metric) is right for the state its author measured — which is not the state the service is in. Landing it unrevised would add counters and a throttle that never engage, and the next reader would again see "the fix is merged" against an unmoved backlog.
- **Scope sketch.** Extend the throttle to the *empty-selection* case: when a cycle selects nothing while the backlog is non-empty, the loop should back off on the same escalation the diagnostic already computes (it is already polling every 30s to re-derive an unchanged answer) — or fold this into proposal 3's latching. Keep the new counters (`roomy_embed_sweep_cycles_total`, `roomy_embed_enriched_{ok,definitive,transient}_total`) unconditionally; they are the part that makes the state readable, and today none of them exists in `/metrics`.
- **Acceptance.** With selectable rows at 0, sweep cycles per minute drop below the idle-poll rate; and `roomy_embed_enriched_ok_total` / `…_definitive_total` / `…_transient_total` are exported and separated in a scrape.
- **Suggested priority:** **P2**.
- **Not already tracked because.** TASK-197 is the task that *produced* `#258`; nothing has measured the state the service is actually in since. (TASK-179/186 are the earlier gauge/cause work; both are done.)

### 6. The notifications page tells an iPhone user to "use Safari 16.1+" while they are in Safari

- **Evidence.** Roomy Space `#general`, 2026-09-24T17:26Z — *dame*: *"i think that worked as a PWA? might want to tweak what the notification page says when in Safari iOS non-PWA mode to indicate that you need to install it as a PWA. Currently it says it works on Safari while I'm in Safari and it's telling me it doesn't work haha. So maybe just 'works on Safari vX when installed as PWA via add to home screen' or something"* (after Zicklag's 16:19Z advice to install the PWA). The same thread's opening question (16:17Z) was *"is there a good way to get push notifications on iOS yet?"*.
  Code: `packages/app-lite/src/routes/user/settings/notifications/+page.svelte:117-121` — when `!pushSupported`, it renders *"Web push isn't supported in this browser. Use a supported browser (Firefox, Chrome, Edge, Brave, or Safari 16.1+) to receive notifications."* `pushSupported` (`:33-37`) is `"serviceWorker" in navigator && "PushManager" in window && typeof Notification !== "undefined"`. On iOS Safari outside an installed PWA, `PushManager` is absent, so the unsupported branch renders **and names Safari 16.1+ as the remedy the user is already using**. The page has no branch distinguishing "this browser has no push" from "this browser needs the app installed first".
- **Why now.** The copy is actively misleading to the population most likely to hit it, it was reported today by a user who then worked it out themselves, and the sibling page has the same vocabulary: `…/settings/notifications/+page.svelte:60-62` says only *"Notifications aren't enabled on this device yet."* Server-side signal agrees the case is common: `roomy-app-lite` push-registration failures are 31 lines / 7d (`AbortError: Registration failed - push service error` 22, `pushManager.subscribe timed out after 20s` 4, `Error retrieving push subscription` 4, `push service not available` 1).
- **Scope sketch.** Split the unsupported message in two by capability, not by browser name: (a) `PushManager` present but permission/registration failed → existing text; (b) `PushManager` absent on iOS/Safari → "on iPhone/iPad, install Roomy to your Home Screen first (Share → Add to Home Screen), then enable notifications from the installed app", with the browser-family list dropped or qualified. Text-only change in `app-lite`.
- **Acceptance.** On iOS Safari (not installed), the notifications page names the install step and does not list Safari as a supported browser; on desktop Firefox/Chrome the existing text is unchanged.
- **Suggested priority:** **P3**.
- **Not already tracked because.** No entry names push onboarding copy. TASK-16/TASK-19 are error-string and re-auth tasks; the 09-23 scout considered push failures and rejected them as browser-side — the *copy* is the actionable half, and it had not been reported then.

## Log findings

- **The embed backlog does not drain.** `roomy_embed_pending` 5,085 (2026-09-22T01:02Z) → **8,167** (now), with the whole gain in one step: 5,116 at 09-23T15:02Z → 6,006 → 7,441 → 7,611 by 18:02Z, then flat-to-creeping (+1 every few hours); `selectableRows 0`, `parkedRows 8,167`, `transientBackoff 7,439`; `enrichedOk 68` vs `enrichedNull 44,610` since the 09-24 restart (~10h) = **0.15% success**; `roomy_embed_backlog_stuck` 1. Interpretation: the 09-23 flood parked every pending URL in the transient window simultaneously, so nothing is selectable and the only movement is new links. Proposals 3 and 5 are the instrument and the fix.
- **`roomy_db_timeouts_total` still has no series** in `/metrics` or Mimir (re-confirmed today, second day). An alert built on it can never fire.
- **`getInvites` 403s — 85 lines / 7d and rising: 7, 15, 2, 8, 12, 15, 26 by day** (26 today, all of them in two bursts: 11 at 13:0xZ and 15 at 15:0xZ). Residual of TASK-113's fix, still live: `packages/app-lite/src/routes/[space]/settings/invites/+page.svelte:11` calls `createInvitesQuery(() => spaceId)` with no `enabled` gate, while the sibling `InviteModal.svelte:20` passes `enabled: () => open`; `queries/invites.ts:24` defaults `enabled` to `true`. The sidebar's `showInvitesTab` (`SpaceSidebar.svelte:145-150`) is a *tab-visibility* gate, and the route is reachable by URL for any member. Not proposed separately because it is a one-line sibling of TASK-161/180/198, the queue already has that shape three times, and `#257` (open) is sweeping the same `queries/*.ts` family — raised here so the rising trend is on the record.
- **`getConnectionTicket` 401s — 29 lines / 7d, and 22 of them today**, arriving in single-client bursts (4 within 16 ms at 09:09:12, 3 at 16:00:00.93, 2–3 at eight other instants). Client side matches: `[sync] Sync connect failed: … getConnectionTicket timed out after 10000ms`, `WebSocket closed before open: code=1006`, `Service auth token is not a valid JWT`. This is the server-side signature of **TASK-19** ("intermittent 'Not authenticated' directly after logging in", P1, open) — recorded as corroboration for that entry, not as a new task.
- **`sendEvents` 403 "Caller does not have write access to this room" — 27 lines / 7d, 16 today**, in one burst of **9 within 26 seconds** (16:45:43.941→16:46:10.040Z, gaps of 1.0–4.1 s) plus a 3-line burst at 09:42–09:43Z. Browser telemetry shows the same strings (5/7d) on three room URLs and one `settings/discord-bridge` page. The composer is meant to be gated on `canWrite` (`ChatInputShell.svelte:260` renders "You don't have permission to send messages in this channel." when it is `false`), and `[room]/+page.svelte:237` computes it as `sidebarRoomInfo?.canWrite ?? roomQuery.data?.canWrite`. Nine retries inside 26 s is a user re-pressing Send at a composer that should not have been enabled. **[INFERENCE]** — I could not attribute the failing rooms (one is a space I am not a member of), and some of the 403s are admin actions (`Failed to grant bot permissions: … Caller is not a space admin` appears in the same 7d window). Re-measure before proposing; today's count is 5× the 09-22 level.
- **`getMessage` "is not a message (no room)" — 145 lines / 7d, last at 2026-09-23T04:52:43Z, 0 since.** TASK-189's fix (`#249`) is still open and unmerged; the errors stopped on their own when the offending caller stopped. See the queue audit.
- **Confirmed-fixed, re-measured today:** `getRoles` 403 **62 / 7d, ending 2026-09-21T03:04Z, 0 since** (TASK-161, `#230`); `createSpace` "request denied by scope policy" **30 request lines / 7d, ending 2026-09-21T05:14Z, 0 since** (TASK-160, `#232`); `Param limit must be ≤ 100` on `getMessages` — **31 request lines / 7d** (plus 31 matching stack echoes), values 200×16, 400×6, 300×3, 101×2, 150×2, and one **1000 as recently as 2026-09-24T06:34Z** — TASK-188 (`#246`) is open and would close it.
- **discord-bridge — 320 error lines / 7d, by day 3, 2, 16, 66, 229, 4.** The 09-23 spike is the structure-sync window: `Cannot resolve name for Discord channel … after 3 attempts; skipping room creation` (49, all 09-23, 14:33Z, guilds 865004050357682246 and 1117291332138958892), `Cannot resolve guildId … skipping backfill` (23, all 09-22), `getMessages failed … Failed to send request to discord` + `Phase-2 backfill failed` in bursts at 05:25Z and 18:32Z (36 pairs), `getPublicArchivedThreads … Missing Access` (7). **Today: 4 lines, all `Connection error for <space>`.** Interpretation: a transient Discord-side degradation during the one-shot structure sync, not a standing defect — rejected as a proposal, and worth revisiting only if it recurs.
- **`No Roomy room mapping for channel … in did:plc:qzie4v7qwnv3mzaflihrf56f, skipping message` — 25 lines, 24 of them 2026-09-23T23:4x–23:59Z, 1 at 2026-09-24T03:59Z.** Every one is the same space and one of two channels. A bridged channel with no Roomy room mapping means those Discord messages are dropped, and there is a `skip_record` written (`message-ingestion.ts:220`) with no surface that reports it. Not proposed: 25 lines, one space, and the same space's `Initial structure synced 12 categories / 97 channels` at 09-23T15:33Z suggests a bridge-config/allowlist mismatch rather than a code defect — but if it recurs after the restart in proposal 2 it is a real "messages silently dropped" signal.
- **`roomy-app-lite` — 1,402 error lines / 7d: 1,252 `ResizeObserver loop completed with undelivered notifications` (89%), 47 `Script error.`, 30 `Permission denied to access property \``, 31 push-registration failures, 6 stale-chunk dynamic-import failures, 2 `MetaMask`, and one each of nine singletons.** Unchanged in shape from the 09-23 report; the ResizeObserver/Script-error classes remain rejected as noise.
- **Mimir:** `roomy_pool_worker_pending` = 0 for all 11 workers; `roomy_search_indexer_queue` 0; `roomy_search_backfilled` 883 → 220 (restart-reset, climbing); `roomy_push_queued` 0; `roomy_cache_size` 491; `roomy_process_starts_total` **1** (no crash loop); `up{job="prometheus.scrape.appserver"}` present.
- **Tempo:** the 3h sample's slowest root spans are `space.roomy.room.getMessages` 283 ms and `space.roomy.space.sendEvents` 185/180/175 ms — nothing pathological; no `getThreads` root span appeared in the window at all.

## Considered and rejected

- **The 09-23 scout's five proposals, re-measured.** `#251`'s proposals 1 (embed churn) and 2 (`getMetadata` retry) now have open fixes (`#258`, `#257`); 4 (duplicate categories) became `#256`+`#261` and is proposals 1–2 here; 5 (arbiter scope error) is folded into TASK-192's Phase 5. Only the parts with new measurements are carried forward.
- **TASK-196's *forward* fix being wrong.** `#256`'s case-insensitive `byName` plus the whole-sidebar `present` guard is correct on the reproduction the 09-23 scout described, and its three regression tests are in the tree. The problem is that it is not running (proposal 2), not that it is wrong.
- **`ResizeObserver loop completed with undelivered notifications` (1,252 / 7d, the largest single signature).** Benign layout race; the app renders in every probe; suppressing it in the Faro instrumentation would be masking. Unchanged from 09-23.
- **`Script error.` (47) and `Permission denied to access property \`` (30).** Cross-origin attribution loss; no stack, no reproduction.
- **Push-registration failures (31 / 7d).** Browser/PDS-side, with no server-side counterpart (0 `scope="push"` errors in the appserver log). Rejected *as a defect* — but the copy that misdirects the user is proposal 6.
- **`roomy_db_timeouts_total` declared but never exported (second day).** Metric hygiene; worth a line in a task about metrics, not its own task.
- **`No Roomy room mapping` (25 lines, one space).** Dropped-message signal worth watching, but the evidence points at a bridge-config mismatch in one space, not code.
- **`~/.roomy/bin/` is still absent on this host** (re-confirmed: `~/.roomy/` contains only `queue.json`, `grafana.env`, `fleet-deploy.log`, `fleet-sdk-build`, `omp-sessions.json`). TASK-141's gap, unchanged, and this run re-derived the CLI/`dev:local`/PR recipes from the brief by hand — the fourth consecutive scout to do so.
- **The 09-24 `ALERT:pr-issues(1)` for `#262`.** Resolved by the 13:16Z and 17:19Z ticks without a dispatch (the PR was already being rebased). Not a scout finding.
- **`#meta` has no new messages** (13 entries, all 2026-09-11). The self-improvement quota has no fresh source today; the nearest new direction is Meri's 2026-09-23 `#coordination` band (progressive scope 05:11Z, bridge backfill UI 05:26Z, getThreads perf 05:31Z, Quint MBT 06:33Z), all four of which she dispatched herself.

## Limits of the evidence

- **No shell on the production host**, so proposal 2's "the fix is not running" rests on the build id the process reports (measured on every line since 17:52Z) plus the single `bridge starting` line in 7 days — not on inspecting the service's image tag or its deploy config, which I cannot read from here.
- **Proposal 1's rollback is measured, its cause is not.** I can show the ref moved forward and back within 2.5 minutes and that the content is absent; I have no reviewer, comment, or CI failure naming a reason. The image workflow for `758ef1b2` *succeeded*, so a build failure on `next` is not the explanation I can see.
- **Proposal 3's mechanism is [INFERENCE] and stated as such in place.** The log volume (2,081–2,640 by window), the 2,640 distinct timestamps, the per-hour histogram, the 396 gauge transitions and the 957/484 sample split are all measured; the *cause* — that a 1→0→1 flap clears `backlogStuck`, which resets `prevCause` to `null`, which makes `null !== "all-parked"` re-log — is read from `sweeper.ts` on this ref and is consistent with the transition count, but I have no shell on the running host and could not reproduce the flap directly. The 24h total is inflated by a three-hour post-restart burst (1,680 lines); the steady rate is 6–45/h and that is the number an acceptance criterion should be set against.
- **Proposal 5 is a code-path argument plus a live snapshot**, not a live reproduction of the empty-selection cycle: I read `sweepYieldsAfter`/`full`/`findPendingLinks` on this ref and read `/health/embed` at the same moment (`selectableRows 0`), which is exactly the state the argument requires — but I did not observe `#258`'s patched code running, since it is unmerged.
- **Proposal 6's iOS claim is the user's report plus the code path.** I cannot test iOS Safari from here; the code branch (`PushManager` absent → unsupported copy that names Safari 16.1+) is unambiguous on this ref, and the reporter is on the device in question.
- **The `sendEvents` 403 room attribution** is explicitly [INFERENCE] (log finding above): I identified three room URLs from browser telemetry but am not a member of the spaces involved and could not reproduce the composer state.
- **`#bugs` has had no new message since 2026-09-18T03:28Z** and `#todos` since 2026-09-16T01:55Z, so those two sources contributed no new community signal today; proposals 6's report comes from `#general`, and the duplicate-category confirmation from `#roomy`.

# Task scout — 2026-09-25

Scout: Bramble.
Ref audited: `scout/2026-09-25` @ `908f3da2` == `origin/next` at the time of the original audit (`git rev-parse --short HEAD` → `908f3da2`). Every file:line in the proposals below was read from that tree; the live-state and carried-forward sections were re-verified against production and the GitHub API at 2026-09-25T20:3xZ, with `origin/next` then at `ff55d802`.
Rooms read: Roomy Space `#bugs` (`01KWF70N615JGPF5EG0HFE7DHP`, last 100 → 2026-08-12T15:34…2026-09-18T03:28Z — **no new message since 09-18**), Meri's Forest `#meta` (`01M278XWXG60HET50VJYVP01YX`, last 100 → **full history, 13 entries, all 2026-09-11**), Meri's Forest `#coordination` (`01M0HME2E3FEYRG72X2YWQRHDZ`, last 25 → 2026-09-22T12:11…2026-09-24T20:14Z; re-read to 2026-09-25T20:16Z for this revision), Muni Town `#roomy` (`01KWF2RMWHD5D6B02PM3SBTMQA`, last 30 → 2026-09-23T01:08…2026-09-24T16:38Z), Roomy Space `#general` (`01KWF70D0M0ZENVQWMYMDSWFZV`, last 30 → 2026-09-17T05:21…2026-09-24T17:26Z), Muni Town `#features` (`01KWF70JH1TTRNV3ANZSXDVR90`, last 25 → 2026-09-03…2026-09-22T14:27Z). Muni Town `#todos` (`01KWF3WPGPSWBN69RPGGGVVF0S2`) 404s — "Room not found" — so the brief's room id no longer resolves.
Logs: Loki `appserver` (7d, 1,104 error lines), `discord-bridge` (7d, 327), `roomy-app-lite` (7d, 2,376 incl. non-level lines), plus per-day offset counts and scope aggregates; Mimir `roomy_*` (22 series, full sweep + 7d range queries); Tempo `appserver` (24h); live `/health`, `/health/embed`, `/metrics`, `roomy.space/build.json`; the fork's PR list and per-PR merge records.
Queue snapshot: 19 open tasks at the original run.

**This document is now the single live record.** Per Meri's 2026-09-25T12:54:39Z process note (`#tasks`), only one task-scout PR is open at a time; the six earlier ones (`#217`, `#228`, `#237`, `#245`, `#251`, `#264`) were closed on 2026-09-25T20:10Z with pointer comments to this PR, and their pending proposals are carried below. Their `scout/2026-*` branches remain, so the original text is still readable; this file restates each pending item rather than referencing it, because the closed PRs are no longer the place a reader will look.

## Ref/live state

- **appserver:** `/health` → `build_id c371a15c`, deployed 2026-09-25T10:31:35Z (`Appserver listening` line). `c371a15c` is `#262` (TASK-199) and is an ancestor of `origin/next`; **11 commits are merged and undeployed** as of 20:3xZ — `#270`, `#271`, `#276`, `#279`, `#255`, `#278`, `#277`, `#267`, `#273`, `#274`, `cbad8cb3`.
- **app-lite:** `roomy.space/build.json` → `commit 1365910b`, built 2026-09-25T16:29:58Z. `1365910b` is the tip of `muni-town/roomy`'s **`main`** and is on **no ref of this fork** (0 of 583 `origin` refs; `git for-each-ref --contains` names only `refs/remotes/upstream/main`). The appserver deploys from `main` too (`c371a15c` = `upstream/main~1`). Six of the 11 undeployed commits touch `packages/app-lite`/`packages/design` (`#271`, `#276`, `#279`, `#255`, `#274`, `cbad8cb3`), and `#271` is the one that stops the two query classes that can never succeed.
- **discord-bridge:** running `build_id d84e2a4d`, unchanged. The only `bridge starting` line in 7 days is 2026-09-23T05:19:59.391Z, so the bridge has been executing 09-23 code for ~2.5 days and is **18 commits behind `origin/next`**, five of them bridge commits (`#255`, `#278`, `#277`, `#267`, `#256`).
- **`#256`, `#261` and the bridge deploy path are all resolved for the record.** `#256` (the forward dedupe fix) is on `next`; `#261`'s branch `fix/task-196-sidebar-recovery` @ `9aea6fbc` still exists on the remote but the merge that recorded it (`758ef1b2`) is on no ref. `#267` records the publish path in the bridge README — **TASK-205 landed**, and it is the documentation half of the deploy gap below, not the fix.

## Carried forward

Every proposal from the six closed PRs, and what state it is in now. Each is restated rather than referenced: the closed PRs are no longer the place a reader will look. Pending items are ordered by the priority their original report gave them.

**Index — 14 items.** Pending, and to be carried: **C1** (`#261` on no ref), **C2** (bridge cannot be redeployed), **C3** (`merge_commit_sha` not durable), **C4** (failed model turn posted as the answer), **C5** (review backlog), **C6** (`#258` cannot fire in production's state — half closed by `#270`), **C13** (scope error has no recovery path — in flight as TASK-192's five open PRs), **C14** (iOS push copy — `#269` open). Dropped, each with its reason stated in place: **C1b** (the malformed-id instance does not reproduce; send-time validation still absent), **C7** (`getActivityFeed` gated — TASK-180), **C8** (the archived-thread window mechanism does not exist in the tree), **C9** (capacity warn is transition+hourly — TASK-162), **C10** (SDK facet validation — TASK-157), **C11** (all three services name their commit — TASK-158), **C12** (marking, prefetch and retry guards all in place — TASK-157/198). Every dropped item is listed rather than deleted, so an omission cannot be mistaken for a judgement; the disposition table below accounts for every proposal from all six closed PRs.

### C1. [#264, 2026-09-24 · P1] PR #261 is recorded `merged` and its code is on no ref — it is the only remedy for the duplicate-category regression, and re-landing could undo a deliberate revert

- **One-line restatement.** The sidebar structure-sync recovery tool was merged by the record and exists on no branch of `next`; either it is re-landed, or the rollback is recorded and it is closed as not-merged.
- **Why still pending.** The rollback's *intent* is undetermined, which makes this Meri's call rather than a worker's. There is no reviewer, no comment, and no CI failure naming a reason: the issue-events feed for `#261` carries exactly `merged` then `closed` at 2026-09-24T07:15:22Z and nothing else, and the image workflow for `758ef1b2` **succeeded**, so a build failure is not the visible explanation. Re-landing is not a neutral act — if the revert was deliberate, re-landing undoes a decision nobody recorded.
- **Evidence, re-verified this revision.** `GET /pulls/261` → `merged: true`, `merge_commit_sha 758ef1b2`, `head_sha 9aea6fbc`, 4 files / +1104. After a full `git fetch origin --prune`: `git cat-file -e 758ef1b2^{commit}` → **object exists**, `git merge-base --is-ancestor 758ef1b2 origin/next` → **NO**, and `git for-each-ref --contains 758ef1b2` → **no ref at all**, from 583 `origin` refs. The same holds for the other two rewritten merges (`ab9a9904` from `#252`, `6eb8d435` from `#256`): object present, on no ref — this is the precision correction below. The branch survives: `refs/heads/fix/task-196-sidebar-recovery` and `refs/pull/261/head` both resolve to `9aea6fbc`, which is *also* on no ref that contains `next`. The content check is independent of the SHA: none of the tool's three source files is on `origin/next`'s tree and `git grep "sidebar-recovery" origin/next -- packages/` returns no match.
- **Precision correction to the previous report's evidence.** The 09-24 report said the merge commit "does not exist". It **does exist as an object**; it is *unreachable from every ref*. The conclusion (not shipped) is unchanged — the reasoning that gets there was wrong, and the coordinator recorded that correction against itself in `#coordination` at 2026-09-25T08:37:29Z. Stating it here so the next reader uses the durable form of the test: `git for-each-ref --contains <sha>`, not `git cat-file -e <sha>`.
- **Scope sketch.** Reconcile the record, not the code — either cherry-pick `9aea6fbc` onto `next` (a standalone ops script; `#256`, the forward fix, is already in tree and touches disjoint files), or record the rollback on TASK-196 and close the PR as not-merged. Whichever happens, the audit consequence is **C3** (the merge SHA must stop being read as proof of shipping).
- **Recommendation (unchanged, still Meri's).** Do not re-land blind. The forward fix is already live-able via **C2**; the recovery tool only matters for the ten guilds already synced before it, and only Meri can say whether the revert was a decision. If the answer is "re-land", it is a 1-commit pick; if "no", the PR should stop reading as shipped.
- **Acceptance.** Either `git grep -l "sidebar-recovery" origin/next` matches, or `pulls/261` no longer reports `merged: true`; and TASK-196's entry names which of the two is true.

### C1b. [#245, 2026-09-22 · P2] Nothing validates a task-dispatch thread id, so a wrong one silently swallows a worker's completion report

- **One-line restatement.** A dispatch naming a malformed thread id produces a mention whose reply path cannot resolve, and the send is refused with no failure signal — so a worker's completion report is dropped while the coordinator reads it as a task that simply never answered.
- **Verdict: implemented — dropped, not carried.** The specific instance does not reproduce, and the durable half is subsumed by the coordinator's own fix. The dispatch that produced the original report named thread `01M33YM8W2HZJ8JCNENXY0HM0` (25 characters against 26 everywhere else), which 404'd on `room.getMetadata`, matched no thread across all 291 in Meri's Forest, and was rejected by `sendEvents` with `400 room must be a valid ULID`. **This task's own dispatch thread resolves**: `01M3BNTEM5QGE8H7ZPY8TJRTD6` reads normally through the CLI, and the coordinator's 2026-09-25T20:16Z tick reports its `read-coord.ts` fix scanning `#tasks` as a secondary room — i.e. the failure mode that *hid* the problem (a directive placed outside the room a tick reads) has been addressed coordinator-side. What has **not** changed: there is still no client-side validation of a room id before a dispatch is sent (`git grep` for a ULID check in `packages/cli/src` finds none), so the check remains a precondition someone must remember rather than one the code enforces.
- **What gave rise to it.** A dispatch that 404s looks exactly like a worker that never finished; the brief's REPORT step names the thread as the only delivery channel, so a bad id silently defeats half the task contract.
- **Honest limit.** I verified the original id's failure and this dispatch's success. I did not re-test the send-time behavior against a deliberately malformed id in this revision, so "no validation exists" is a code read, not a reproduction.

### C2. [#264, 2026-09-24 · P1] The discord-bridge deploy path cannot deliver a `next` build — the bridge has run 09-23 code for 2.5 days and TASK-196's merged dedupe fix is not executing

- **One-line restatement.** The bridge process is `d84e2a4d` while its fixes are merged on `next`; nothing in the repo can advance the running service, and the fix that stops the duplicate-category regression therefore is not running for any bridged space.
- **Why still pending — the ops half is now proven, and it is not fixable from here.** This is the item the coordinator marked "Ops, not code ... Meri's call", and the evidence has since got stronger in both directions: the *documentation* half landed (`#267`, TASK-205 — the README now records the publish path and the merged-vs-running check), and the *execution* half is still impossible from any of these VMs.
- **Evidence, re-verified this revision.** Running process: 17 of 17 `discord-bridge` lines in the last 30 minutes carry `build_id d84e2a4d`; the 7d query for `msg="bridge starting"` returns **exactly one** line, 2026-09-23T05:19:59.391Z. `git rev-list --count d84e2a4d..origin/next` → **18**, of which five are bridge commits (`#255`, `#278`, `#277`, `#267`, `#256`). The repo side, read from `origin/next`: `.github/workflows/discord-bot.yaml` triggers on `main`/`next`/`v*` but its single job is `build-and-publish` (checkout → metadata → Buildx → login → push) with **no** deploy step, no `RAILWAY_TOKEN`, no `railway up`; there is no `railway.json`/`railway.toml`/`nixpacks.toml`/`Procfile` and no root `Dockerfile` in the tree; and on this host `which railway` → nothing and `~/.railway` → absent.
- **Why it still matters, and the one thing that changed.** `#256`'s dedupe is the forward fix for a regression a user confirmed (`#roomy`, 2026-09-24T14:02Z: *"Yeah i see that in my server"*). The state is unchanged: every further restart of the *old* image re-applies the bad merge path. What changed is that `#267`/TASK-205 **documented** the gap — so "merged" vs "running" is now checkable from inside the repo. That is the part a worker could do, and it is done. What remains is a service-side rollout only Meri can perform.
- **Scope sketch.** No bridge code. (a) Advance the running service to the published `next` image; (b) decide whether a `next` push should deploy itself — that needs a deploy step the repo does not have (platform auto-update on the service, or an explicit hook) — and if so, add it, because otherwise this recurs on every bridge merge.
- **Acceptance.** A `bridge starting` line carrying a build id that is an ancestor of `next` and ≥ the newest merged bridge commit (`2aef2cf6`), and the affected spaces render one set of categories.
- **Honest limit.** I can prove the process's build id and the absence of any in-repo deploy step. I cannot see the platform's service settings — the image reference and any auto-update schedule — from here, and those would settle it in one look. **Not re-litigated; recorded as pending, per instruction.**

### C3. [#264, 2026-09-24 · P2] `merge_commit_sha` is not a durable identifier here — three of the last five merges to `next` name a commit that is on no ref

- **One-line restatement.** The shipped-work audits decide "this shipped" from a merge SHA; on this repo that SHA is frequently unreachable, so the predicate should move from SHA reachability to content presence.
- **Why still pending.** It is a coordinator-side monitor gap, and the coordinator has not fixed it. It is unfixed **as of this revision**, which is why it is carried.
- **Evidence, re-verified this revision (with the precision correction).** The five most recent merges to `next` and what the API recorded: `#247` → `d84e2a4d` (on `next`), `#248` → `2d5051dc` (on `next`), `#252` → `ab9a9904`, `#256` → `6eb8d435`, `#261` → `758ef1b2`. The last three are **present as objects but contained by no ref** (`git cat-file -e <sha>^{commit}` → yes; `git for-each-ref --contains <sha>` → empty; `git merge-base --is-ancestor <sha> origin/next` → no). Their content survives only where the same change was re-committed — `#252`'s as `d24cb28f`, `#256`'s as `908f3da2`, both ancestors of `next`; `#261`'s nowhere. Today's merges are clean under both tests (`#270`→`ff55d802`, `#271`→`b57b60c4`, `#274`→`61e6d7e1`, `#276`→`3ae5cff9`, `#279`→`e059b69c`, `#273`→`5b5fb375`, `#255`→`2aef2cf6`, `#277`→`b288a68f`, `#278`→`ab4d69a6`, `#267`→`5c68f266` are each `merge-base --is-ancestor`-true of `next`), so this is a hazard, not a standing break — it bites exactly when `next` is rewritten, which is what happened on the morning of 09-24.
- **Scope sketch.** Change the predicate from SHA reachability to content presence: for a PR reported merged, assert that a distinctive symbol from its changed-file set exists on the shipped ref, and emit a token when it does not. Optionally have the digest report the reachability of each merge SHA it records, so "merged" and "on `next`" stop being treated as one fact. Do not build a general monitor; this is a predicate change in one place.
- **Acceptance.** Running the new check against the 09-24 list flags `#261` (and would have flagged `#252`/`#256` before their re-commit) and is silent for `#247`, `#242`, `#241`, whose merge SHAs *are* ancestors of `next`.

### C4. [#228 / TASK-159 (#229), 2026-09-18 · P1] A failed model turn is posted as the answer — the raw-tail fallback defeats the empty-reply guard, and it fired fleet-wide again on 2026-09-25

- **One-line restatement.** When a model turn fails, `runOmp` salvages the last 2 KB of raw stdout as the "answer", so the responder posts a slice of its own error transcript into the thread as if it were the reply — and the queue records `done` with every digest `ok`.
- **Why still pending.** `#229` is **open, `mergeable: clean`, and 7 days old** (created 2026-09-18T08:21Z). Nothing has merged it, and the mechanism fired again today.
- **Evidence.** The defect is unchanged on `origin/next` (`packages/cli/src/omp.ts`): on exit 0 with no structured answer, the no-text branch resolves with `const fallback = (outTail.text.trim() || errTail.text.trim() || "").slice(-2000)`, so `respond.ts`'s non-empty-answer guard can never fire. `#229` adds `failure` to `parseOmpJson`, rejects in `runOmp` on a terminal `stopReason: "error"`, and its own test fixtures reproduce the 429 transcript. **This run's live instance**, from `#coordination` 2026-09-25T08:33:31Z: the model `deepseek-v4-flash:0731` was retired at 2026-09-25T00:00 PDT; two hosts (meri-agent-2/3) whose `~/.omp/agent/models.yml` pinned it produced turns with `contentBlocks:0, hasText:false`, and their "reports" were **the brief tail verbatim**. The coordinator's own words: *"Every surface read healthy — bridge active, responder alive, queue `done`, socket connected, `tree=ok`. **None of the nine digests can see this.**"* The two hosts were repointed by hand and the tasks re-dispatched, so the *hosts* are fixed; the *code path* is not.
- **Note on this document's own status.** The failure class is live in a way that touches this very task: a worker whose turn fails posts a tail, and the queue reads `done`. That is why the mechanism is ranked as the strongest carried item rather than a backlog nicety.
- **Scope sketch.** Two separable changes, both in `#229` already: (1) in `omp.ts`, distinguish "no structured answer **and** a terminal error event in the stream" from "no structured answer but omp succeeded" — reject for the former so the responder's existing per-job catch records `failed` and posts nothing; keep the raw-tail fallback only for the benign case it was written for. (2) do not widen into retry/backoff policy — a 429/410 here is a hard quota or a retirement, not a transient.
- **Acceptance.** A unit test driving `runOmp` with a terminal `errorStatus` stream that asserts it **rejects** (fails pre-fix, since today it resolves with the raw tail); plus one observed tick that either posts a real answer or records a `failed` job and posts nothing.
- **What is NOT this proposal.** The stale local `models.yml` files are a host-config question the coordinator raised separately and answered by hand. This proposal is only the code path that turns any such failure into a plausible-looking post.

### C5. [#251, 2026-09-23 / #264, 2026-09-24 · P2] The standing review backlog — 19 open PRs, many green for days, and one of them is the P0 fix for TASK-159

- **One-line restatement.** A large set of mergeable, CI-green PRs sits unreviewed; five days of scout reports have flagged it and `#229` (TASK-159, the P0 above) is now 7 days old.
- **Why still pending.** It is process, and it needs Meri. Two of the oldest are decisions, not code: `#229` (TASK-159) and `#234` (TASK-169).
- **Evidence, re-verified this revision.** 19 open PRs; **15 of them not scout reports**. Every one reports `mergeable: true` / `mergeable_state: clean`. The oldest five: `#227` (2026-09-16, TASK-138's plan, 9 days), `#229` (2026-09-18, TASK-159, 7 days), `#234` (2026-09-18, TASK-169, 7 days), `#246` (2026-09-22, TASK-188), `#249` (2026-09-22, TASK-189). The rest are the five `TASK-192` scope PRs (`#253`, `#259`, `#260`, `#263`, `#265`), `#258` (TASK-197), and today's `#269`, `#272`, `#280`, `#281`, `#282`, `#283`, `#284`. **The direction of travel is healthy on the merge side** — eleven merges landed today — so this is specifically an *old-tail* problem, not a throughput one: the five oldest are the five nobody has decided about.
- **Scope sketch.** Not a code change. Either review-and-merge the green set, or record a rule for how long a green PR may sit before it is merged or closed — which is the decision TASK-142 asks for. Two of the five are cheap: `#234` and `#246` are `packages/cli` fixes whose acceptance is measurable in a log query; `#229` is the item above.
- **Acceptance.** The open-PR count is a number the daily scout can re-measure and the oldest age is bounded by a stated rule.
- **Honest limit.** "No reviewer is looking" is inferred from activity, not from a review record I can read.

### C6. [#264, 2026-09-24 · P2] `#258` cannot fire in the state production is actually in — and the empty-selection throttle was the missing half

- **One-line restatement.** TASK-197's throttle (`sweepYieldsAfter` engages only when a *full* batch produces no `ok`) cannot engage while every parked URL is unselectable; the state production is in is the one it does not touch.
- **Why still pending — and what changed.** The gap was real and is now **half closed by a different PR**. TASK-201 / `#270` merged today (2026-09-25T13:58:46Z, `ff55d802`, currently undeployed — see the ref/live state) adds exactly the missing half: an escalating poll for the stalled path (30s → 60s → 120s → 240s → 300s, capped by `EMBED_STALL_POLL_MAX_MS`), backoff persistence from `comp_embed_link_data`, and a latched stall log. `#258` itself is **still open and clean**, unchanged since 2026-09-23T13:27Z, and now overlaps `#270` in one file (`packages/appserver/src/embed/sweeper.ts` plus `sweeper.test.ts`).
- **Evidence, re-verified this revision.** `/health/embed` at 20:2xZ: `pending 8241, selectableRows 0, parkedRows 8241, backoffUrls 7513, inFlight 0, backlogStuck true, lastStallCause "all-parked"`, lifetime `enrichedOk 90` against `enrichedNull 45108`. With `selectableRows 0`, `findPendingLinks` returns 0 rows, `pending.length < SWEEP_BATCH`, so `full=false` and `#258`'s `sweepYieldsAfter` returns false — the branch it does not touch. `git show origin/next:…/sweeper.ts | grep -c sweepYieldsAfter` → **0**, and `grep -c sweep_cycles_total` on `appserver.ts` → **0**, so `#258`'s counters are still absent from the tree and from `/metrics`.
- **Scope sketch.** Rebase `#258` on `#270` and re-scope it to what it still uniquely contributes — the counters (`roomy_embed_sweep_cycles_total`, `roomy_embed_enriched_{ok,definitive,transient}_total`) and the full-batch yield — rather than re-adding the empty-selection throttle `#270` now owns. Landing it unrevised would add a throttle that cannot engage and duplicate `#270`'s change.
- **Acceptance.** `roomy_embed_enriched_ok_total` / `…_definitive_total` / `…_transient_total` are exported and separated in a scrape, and the full-batch yield is bounded, on a tree that already carries `#270`.

### C7. [#237, 2026-09-21 · P2] The home page fires `getActivityFeed` before auth lands — the unauthenticated 401 storm

- **One-line restatement.** `createActivityFeedQuery` issued its request before `auth.authenticated` flipped, producing the largest appserver error signature of the week.
- **Verdict: implemented — dropped, not carried.** Stated explicitly because an item that disappears from a carried list without a reason is indistinguishable from an item that was forgotten. The fix is on `origin/next` (`queries/activity-feed.ts`, TASK-180/`#239`): `enabled: () => auth.authenticated` and `retry: false`, with the rationale in-comment. Measured: `getActivityFeed failed` → **1 line in 7d**, last at 2026-09-21T07:xxZ, against 10,955 in the week the proposal was written.
- **What gave rise to it, recorded as context (Meri's "VERY important" clause).** A single night's 401 burst on 2026-09-15: 1-second buckets showing 58 requests inside one second at 22:22:42Z and **556 in one second** at 22:22:59Z, then ~15 every 2 seconds — the home route mounting `<ActivityFeed>` as soon as spaces loaded, while `requireAuth` only flipped on the first navigation. It became a task because an unauthenticated page view was the app's most common entry and generated an order of magnitude more errors than any other signature. The documentation for that belongs in its own PR against the handler/query, not here.

### C8. [#217, 2026-09-16 · P2] Discord-bridge archived-thread backfill fails on Discord's 15-minute `before` window

- **One-line restatement.** `getPublicArchivedThreads` pagination passed a `before` derived from the thread's `archive_timestamp`, which Discord rejects once it is older than ~15 minutes, silently truncating bridged history.
- **Verdict: not carried.** My correction to the proposal's mechanism, and why it is dropped. The failure signature has **essentially stopped** (5 `getPublicArchivedThreads failed` lines / 7d, last 2026-09-23T19:11:45Z; 3 `backfill archived threads` / 7d, same last timestamp), and the remaining ones are a *different* cause from the one proposed: they carry `(discord status 403: {"message": "Missing Access", "code": 50001}), attempt 1; not retrying (non-retryable)` — a permissions failure on channel `1473104502436528138`, not a window rejection. Reading the code on `origin/next`, there is **no `threadTimestampToCursor` function anywhere in the tree** (`git grep -n threadTimestampToCursor origin/next -- packages/` → no match; the only hit across all refs is the 09-16 scout document itself), and the archived-thread loop advances its cursor from `lastThread.id` with an explicit non-advance guard (`Archived thread pagination stalled for parent channel … cursor … did not advance; stopping`, 0 occurrences in 7d). So the specific mechanism the proposal named **does not exist in the code as shipped**, and the surviving failures are 403s.
- **What is real and separate.** The surviving failures are a **403 `Missing Access` on one mapped parent channel**, not a cursor problem: the bot cannot read that parent, the call fails fast on the first attempt by design (TASK-156/`#220` made deterministic 4xx non-retryable), and history for it is not bridged. That is a permissions/config question for whoever owns the Discord side, recorded here as a finding rather than proposed.
- **What gave rise to it, recorded as context.** The 09-16 scout measured this as the single most frequent bridge error (`before=-` on the failing calls, 21 of 24 hourly buckets, peak 63/h) and traced a `threadTimestampToCursor` helper that has since been replaced by the `snowflakeToEpochMs` fix in TASK-124/`#210`. That history belongs with the bridge's archived-thread code, not here.

### C9. [#228, 2026-09-18 · P3] The grant-less capacity warn is a large share of bridge log volume

- **One-line restatement.** The bridge emitted "no capacity to enforce — failing open" on every ~5-minute sweep, making it the largest reducible block in the bridge's log stream.
- **Verdict: implemented — dropped, not carried.** TASK-162/`#231` is on `origin/next` and is **running** on `d84e2a4d` (which is `#247`, merged before it): `capacity.ts` now carries `FAIL_OPEN_SUMMARY_INTERVAL_MS = 3_600_000`, logs the per-tuple line on *transition* (`firstEntry`) and the summary at most hourly, and the summary line is what production now emits. Measured this revision: `no capacity to enforce` → **1 line in 1h, 6 in 6h, 24 in 24h** (24 = one per hour across 10 fail-open pairs), against 2,623–3,970/day before the fix. Dropped as a pending proposal.
- **What gave rise to it.** 24,921 lines in 7 days — 38% of all bridge log volume — for a state that changes only when a grant ships, so the same warning was re-emitted ~288 times/day/guild to say nothing had changed. It became a task because the noise made a real capacity transition impossible to spot in Loki.

### C10. [#217, 2026-09-16 · P3] The client's link prefetch trusted stored `#roomRef` facets that were never validated

- **One-line restatement.** `extractInternalLinkTargets` in the SDK pushed every stored `#roomRef` facet through with no DID/ULID validation, while the app-lite path validated — so stale facets drove 404 `getSpaceSummary` prefetches forever.
- **Verdict: implemented — dropped, not carried.** On `origin/next`, `packages/sdk/src/richtext/convert.ts` now gates on `Did(roomRef.spaceId)` and `Ulid(roomRef.roomId)` before pushing (TASK-157/`#219`), with the parity rationale in-comment. Corroborating measurement: `getSpaceSummary failed` fell to 8/7d on `908f3da2` and `getRoomSummary failed` to 4/7d, from 143+127 in the week the proposal was written. Dropped as a pending proposal.
- **What gave rise to it.** The 2026-08-17 fix (`99039009`) hardened only the href layer; the renderer's internal-link marking and the facet-read path were left unvalidated, so links persisted before that date kept producing 404 summary fetches.

### C11. [#217, 2026-09-16 · P3] No production service reported which commit it was running

- **One-line restatement.** `/health`, the bridge's logs, and the app-lite bundle each failed to name their commit, which made "is my fix live?" unanswerable.
- **Verdict: implemented — dropped, not carried.** All three now do. appserver `/health` → `build_id c371a15c` (a real 40-char-containing id, stable across reads this run); discord-bridge lines carry `build_id d84e2a4d`; app-lite serves `roomy.space/build.json` → `{"commit":"1365910b","version":"0.2.0","built_at":"2026-09-25T16:29:58.970Z"}`. Dropped as a pending proposal — and it is the instrument that made every other finding in this document measurable, including **C2**, which it does not fix.
- **What gave rise to it.** Three independent gaps verified in one run: appserver `/health` reading `build_id: "unknown"` from a 04:03Z deploy, the bridge `unknown` on all 28,980 lines of a 7-day window, and app-lite inlining no commit at all — while a just-merged behaviour change could only be inferred from a build *timestamp*, never confirmed against a commit.

### C12. [#217, 2026-09-16 / #228, 2026-09-18 · P2] The client fired space/room summary queries for URL text that was never a room link

- **One-line restatement.** Markdown marked anything under a known Roomy domain as an internal link, so URLs like `https://roomy.space/profile` mounted a badge and issued a summary query that could only 404 — amplified by TanStack's default `retry: 3`.
- **Verdict: implemented — dropped, not carried; a small live residual is recorded as a finding instead.** On `origin/next`: `design/src/utils/markdown.ts` gates marking on `isSpaceRoomPath` (a `Did`/`Ulid` check), `enrich-internal-links.ts` validates via `parseInternalLinkHref`, and `SpaceRoomBadge.svelte` sets `retry: false` on both summary queries. `getMetadata failed` → **0 lines since the `c371a15c` deploy at 2026-09-25T10:31Z** (was 72/7d), which is TASK-198/`#257` landing. Residual, unchanged and non-zero: `getSpaceSummary failed` 47/7d and `getRoomSummary failed` 28/7d with the newest at 2026-09-25T13:xxZ, dominated by `watch` (19) and `p` (17). Dropped as a pending proposal; the residual is folded into the log findings, because the marking, prefetch and retry guards are all in place and the counts are an order of magnitude below the proposal's 404/24h.
- **What gave rise to it.** 73 404s in 24h across 19 targets, all non-DID path segments, one page load producing six of them.

### C13. [#251, 2026-09-23 · P2] An admin's "create Space card" fails on an OAuth scope error with no recovery path

- **One-line restatement.** A session issued before a scope was added cannot acquire it without a fresh authorization, and the app has no path to one — so an admin gets a bare toast naming a missing scope and the feature is unusable for them.
- **Why still pending.** It is **in flight, not stalled**: TASK-192 (progressive scope expansion) was dispatched off this proposal and its five phases are open PRs — `#253` (P1 server-side scope storage), `#259` (P2 client scope refactor), `#260` (P3 grant tracking), `#263` (P4 metadata ceiling + settings), `#265` (P5 reactive consent dialogue). All five are `mergeable: clean` and open. The concrete instance is therefore being fixed by a general mechanism rather than by a point fix, which is the right shape; it is carried so the *instance* is not lost while the mechanism lands.
- **Evidence, re-verified this revision.** The one real occurrence in the browser telemetry is still 2026-09-22T21:38:53Z: `console.error: createSpaceCard failed: Error: Missing required scope "rpc:space.roomy.authComplete.arbiter.proxy?aud=did:web:arbiter.muni.town"`. The other hits for that string are **agent prose** echoed by `scope="push-evaluate"` (2026-09-23T07:12–07:22Z on `d84e2a4d`, 2026-09-24T20:15–20:39Z on `908f3da2`, 2026-09-25T12:23Z and 20:22Z on `c371a15c`) — my own fleet's messages discussing the finding, which is the contamination trap the 09-22 scout named. Net: **1 real occurrence**, unchanged, and Meri's own reply at 2026-09-22T21:41Z (*"you need to log out and in - again we need to fix this"*) remains the corroboration that it is not a one-off.
- **Scope sketch.** No new work — reconcile with TASK-192's phases. `#259`/`#260` cover the client-side refactor and grant tracking; `#265` is the consent dialogue that would offer the re-authorize. The one thing worth checking when those land: the failing call site is `ChatMessage.svelte`'s `handleCreateCard`, which catches and reports via `console.error` + `toast.error`, so the recovery has to be reachable from a *toast*, not only from a settings page.
- **Acceptance.** An admin whose session predates the scope can use the button after one prompt, without logging out; and no path reports a missing scope as a bare toast.
- **Not already tracked because.** TASK-16 covers a different error string and trigger. TASK-192 is the umbrella; this is its first concrete instance and it is carried here so the instance survives the umbrella's landing.
- **Honest limit.** One occurrence is one occurrence. I did not reproduce it — it needs a session issued before the scope was added, which I cannot construct from here.

### C14. [#264, 2026-09-24 · P3] The notifications page tells an iPhone user to "use Safari 16.1+" while they are in Safari

- **One-line restatement.** The unsupported-browser branch renders for iOS Safari outside an installed PWA — naming Safari as the remedy the user is already using — because the page has one message for every device without `PushManager`.
- **Why still pending.** The fix exists and is unreviewed: **`#269`** (`sorrel/task-204-ios-push-copy`, created 2026-09-25T08:37Z, `mergeable: clean`, open) splits the message by capability rather than by browser name. It is a **C5** item — the oldest-tail review backlog — with a single-file, text-only diff.
- **Evidence, re-verified this revision.** On `origin/next`, the notifications page still renders the single message in its `{#if !pushSupported}` branch, and `pushSupported` is still `"serviceWorker" in navigator && "PushManager" in window && typeof Notification !== "undefined"` — so on iOS Safari outside an installed PWA the branch renders and names `Safari 16.1+`. The report stands: Roomy Space `#general`, 2026-09-24T17:26Z, *dame*: *"it says it works on Safari while I'm in Safari and it's telling me it doesn't work haha."*
- **Scope sketch.** None beyond reviewing `#269`. Its own PR body states the split (installed-PWA advice vs. the existing browser-list text) and keeps desktop Firefox/Chrome output unchanged.
- **Acceptance.** On iOS Safari (not installed), the page names the install step and does not list Safari as a supported browser; on desktop Firefox/Chrome the existing text is unchanged.
- **Not already tracked because.** No entry names push onboarding copy; TASK-16/TASK-19 are error-string and re-auth tasks.
- **Honest limit.** The code branch is unambiguous on this ref; the device behaviour is the reporter's account, and I cannot test iOS Safari from here.

### Disposition of every proposal from the six closed PRs

Thirty-three proposals across `#217`, `#228`, `#237`, `#245`, `#251` and `#264`, each accounted for. "Implemented" means the fix is in tree; where it is merged but **not running** the row says so, because that is this PR's central finding.

| From | Proposal | Disposition |
|---|---|---|
| #217 | DB query timeouts / 30 s timeouts across 8 routes | Implemented — TASK-155, `#221`/`#236`. 12/7d now, and the two on the current build are `admin.listSpaces`, a different handler |
| #217 | Archived-thread backfill 15-min window | **Dropped — C8**: the mechanism no longer exists in the tree |
| #217 | Summary queries for non-room URL text | **Dropped — C12** (residual recorded as a finding) |
| #217 | No service reports its commit | **Dropped — C11** |
| #217 | 15 handlers await a discarded membership read | Implemented — TASK-155, `#221`; `hydrateUserMembership` is gone from `getMessages` |
| #217 | Unvalidated `#roomRef` facets drive prefetch | **Dropped — C10** |
| #228 | `createSpace` 500s / arbiter scope policy | Implemented — TASK-160, `#232`. Last occurrence 2026-09-21T05:14Z, **0 since** |
| #228 | Failed model turn posted as the answer | **Carried — C4** (`#229` open, 7 days; fired again 2026-09-25) |
| #228 | `getRoles` fires unconditionally on space entry | Implemented — TASK-161, `#230`. Last occurrence 2026-09-21T03:04Z, **0 since** |
| #228 | Capacity warn is 38% of bridge log volume | **Dropped — C9** |
| #228 | `vite:preloadError` unhandled | Implemented — TASK-163, `#233`; the listener is in `error-recovery.ts` |
| #237 | Embed stalled + the gauge is wired to the wrong number | Implemented (`#240` gauge, `#248` cause) — **residual carried as C6 / proposal 3** |
| #237 | `getActivityFeed` issued before auth lands | **Dropped — C7** (1 line / 7d) |
| #237 | Bridge fixes cannot reach the bridge | Implemented (`#243` publication) — **the rollout half is C2** |
| #237 | `getMessage` 400s for self-room reply targets | Implemented — TASK-182, `#242`; the client residual is `#249` (open) |
| #237 | Invite links are `tauri://localhost/…` | Implemented — TASK-183, `#241`; no `location.origin` builder left |
| #245 | Embed backlog stalled with rows outside every window | Superseded — TASK-179/186; the drain half is **C6 / proposal 3** |
| #245 | Reply targets that are not messages after `#242` | `#249` open, `mergeable: clean` — a **C5** item |
| #245 | Deployed build behind `next` on a latency fix | Implemented; the general form is **proposal 1** |
| #245 | CLI `--limit` over 100 is a hard 400 | `#246` open — a **C5** item, and **TASK-188's record is a queue finding** |
| #245 | Nothing re-fetches a profile that already has a row | Implemented — TASK-187, `#247`, merged 2026-09-23 and running |
| #245 | Nothing validates a dispatch thread id | **Dropped — C1b** (send-time validation still absent) |
| #251 | Embed sweeper runs its batch back-to-back | Implemented — TASK-201, `#270` (merged, **undeployed**) — **proposal 3** |
| #251 | `getMetadata` retries a 404 four times | Implemented — TASK-198, `#257`, deployed; **0 lines since 10:31Z** |
| #251 | Nine green PRs with no reviewer | **Carried — C5** (now 19 open, 15 non-scout) |
| #251 | Discord structure sync duplicated every category | Implemented (`#256`) — **the running half is C2**; the remedy tool is **C1** |
| #251 | Arbiter scope error on "create Space card" | **Carried — C13** (TASK-192's five phases, all open) |
| #264 | `#261` recorded merged, code on no ref | **Carried — C1** |
| #264 | Dedupe merged, bridge still executing the buggy merge | **Carried — C2** |
| #264 | Stall diagnostic repeats because a flap resets its latch | Implemented — TASK-201, `#270` (merged, **undeployed**) — **proposal 3** |
| #264 | `merge_commit_sha` is not durable | **Carried — C3** |
| #264 | `#258` cannot fire in production's state | **Carried — C6** (half closed by `#270`) |
| #264 | iOS notifications copy names Safari to Safari users | **Carried — C14** (`#269` open) |

Nothing in the six closed PRs is unaccounted for. **Six items are pending** and named as carried (C1, C2, C3, C4, C5, C6, plus C13 and C14 which are in-flight in open PRs rather than undecided) — the eight headings above are the pending set; the other rows are implemented or dropped with a reason. The four findings that are *not* proposals — TASK-169/188's record, TASK-21~TASK-138, TASK-63, TASK-166(b) — are in the queue-findings section.

## New proposals (ranked)

These are this run's own findings, renumbered, each re-verified against `origin/next` at `ff55d802` and against production at 20:3xZ. Of the six filed in the first revision of this PR:

| #266 proposal | state now |
|---|---|
| 1. bridge deploy path | **carried** as **C2** |
| 2. jitter draw of 0 | **fixed** in `#278` (merged, undeployed) — see the note below |
| 3. embed backoff is process-local | **fixed** in `#270` (merged, undeployed) — see the note below and proposal **3** |
| 4. invites route fires a doomed query | **fixed** in `#271` (merged, undeployed) — proposal **2** is its rollout |
| 5. reply-preview re-issue | **fixed** in `#271` — see the note below |
| 6. URL slugs reach id-expecting params | **half shipped**: `#257` deployed; `#272` in review — see the note below |

### Original #266 proposal 2 — implemented, merged, and not deployed

The zero-jitter hazard (`Math.random()` can draw exactly 0, and the SDK reads a non-positive delay as its documented "stop reconnecting" signal, so an unlucky draw would wedge a bridged space for the life of the process while every liveness probe read healthy) is fixed in **`#278`** (`ab4d69a6`, merged 2026-09-25T12:59:10Z): `return Number.isFinite(jitter) ? Math.max(1, jitter) : 1;`, plus a regression test, and the worker also caught that `Math.max` does not absorb `NaN` from an unvalidated env override. **What gave rise to it:** reading the bridge's own reconnect generator against the SDK's — the SDK's default had been floored for precisely this incident, and its in-code note names the production case (a bridge logging `Closed: code=1006 intentional=false` and then never logging a reconnect attempt again while its process stayed alive). The bridge's copy predated that fix and was never updated, and its unit test pinned the zero as correct behaviour. It has **never fired** — a 7-day query for the SDK's stop-signal strings returns 0 lines across all services — and it is **merged and not running**, which is why it belongs to proposal 1's rollout rather than to a proposal of its own.

### Original #266 proposal 3 — implemented, merged, and not deployed

The backoff-durability defect (state held only in an in-memory `Map` while `comp_embed_link_data.retry_after`/`attempts` were written and never read, so every restart made the whole backlog selectable at once) is fixed in **`#270`** (`ff55d802`, merged 2026-09-25T13:58:46Z): `seedTransientRetry` restores unexpired windows at boot and carries `attempts` over, the stall log is latched, the empty-selection poll escalates, and the restart-amplification is closed. **What gave rise to it:** watching the same backlog survive every deploy while `enrichedOk` stayed in the tens — a restart cost a full-backlog refetch at a ~0.2% success rate, which is why the backlog never drained across releases. It is **merged and not running**, and its acceptance (a first cycle with `selectableRows` materially below `pending`) is the one measurement that cannot be taken until it deploys — proposal 3 asks for exactly that.

### Original #266 proposal 5 — implemented, merged, and not deployed

The reply-preview amplifier is fixed in `#271` (`b57b60c4`, merged 2026-09-25T13:57:59Z): `retry: false` stopped the retryer, but TanStack's `shouldLoadOnMount` still re-issued a fetch for an errored, data-less query on every virtualizer recycle, so `#271` adds `retryOnMount: false` beside it in `queries/message.ts`. **What gave rise to it:** probing the reported 400 against production showed the *request count* was the live problem rather than the bad data — one doomed target was re-asked once per row recycle — and the read-side 400s had already stopped (TASK-182's write guard), so the re-issue was the only part left to fix. Its verification is its author's (control 1 + 18 requests for the doomed target, exactly 1 per target with the change, 0 on recycle) and it is **not running** — see proposal 1.

### Original #266 proposal 6 — the retry half is deployed, the value-resolution half is in review

`getMetadata`'s `retry: false` + `enabled` gate landed in `#257` (TASK-198, `fc8a4586`, merged 2026-09-25T10:10:06Z) and is **deployed** — `c371a15c` descends from it — and the signature confirms it: `getMetadata failed` → **0 lines** since the 2026-09-25T10:31Z deploy, against 72 / 7d before it. The half that resolves *what value is sent* is **`#272`** (TASK-203), open, `mergeable: clean`, created 2026-09-25T09:12Z: URL-derived entry points pass a route segment into an id-expecting param. **What gave rise to it:** four days of `Space not found: home` / `watch` / `p` at 20–52/day, with `blento.app` and `atmosphereconf.org` among the targets — third-party links, not only user error — and only `join/+page.svelte` demonstrably able to send a URL segment to `getMetadata`. Recorded here rather than re-proposed: `getSpaceSummary` still 404s on `watch` (19/7d) and `p` (17/7d), the same slug class reaching a second handler that `#272` does not cover.

### 1. The appserver and app-lite deploy from `main`, not `next` — 11 merges are live-in-`next` and not running, including the two query guards and the embed-backoff fix

- **Evidence.** appserver `/health` → `build_id c371a15c`, deployed 2026-09-25T10:31:35Z. `c371a15c` is **`upstream/main~1`**; the appserver's `Appserver listening` history for the last 7 days ends `…908f3da2 (09-24T07:15) → c371a15c (09-25T10:31)`, i.e. two deploys in two days while `next` took eleven merges. app-lite serves `commit 1365910b` = **`upstream/main` tip**, which is on **no ref of this fork** (0 of 583 `origin` refs; `git for-each-ref --contains 1365910b` names only `refs/remotes/upstream/main`). `git rev-list --count 1365910b..origin/next` → **11**, and six of those touch `packages/app-lite`/`packages/design`.
  What is not running that matters, with its measured signature still arriving: **`#271`** stops the two query classes that can never succeed, and production still emits `Member-created invites are disabled in this space` **46 lines in the 24h since the c371a15c deploy** (215 / 7d) plus `Reply target … is not a message` **10 lines on `c371a15c`** (22 / 7d); **`#270`** persists the embed backoff and throttles the stalled poll; **`#273`** batches sendEvents authorization through one access memo; **`#255`** carries the Discord backfill status UI Meri refined at 13:47Z today.
  The `#271` case is the sharpest: its own PR body reports verified numbers — control 1 + 18 requests per virtualizer recycle for a doomed `getMessage`, zero after; a 403 rendered as a raw error before, an explicit permission notice after — and none of that is in front of a user, because the deployed app-lite is `main` and `#271` is on `next`.
- **Why now.** Eleven merges of measured work are one deploy away from a user, and the *reason* they are invisible is a ref mismatch nothing in the repo states. This is the same class as **C2** (the bridge) one layer up, and it is the mechanism behind every "the fix is merged" that a scout report has to qualify. The evidence says production follows `upstream/main`, which is a *different repository's* branch — so "the fix is on `origin/next`" and "the fix is live" are not the same statement, and no scout has been able to say which ref the deploy watches.
- **Scope sketch.** A decision first, then a one-line change. (a) Decide the deploy ref: if production should follow `next`, point the deploy at it; if it should follow `main`, then `main` must be advanced (or fast-forwarded) from `next` as a release step, and the repo should say so. (b) Record the answer where a reader will find it — the app-lite/appserver READMEs currently record nothing about the deploy trigger, while `packages/discord-bridge/README.md` now does (TASK-205/`#267`), so the three services disagree about whether this is documented. Do **not** treat "merge `next` into `main`" as the fix on its own; without (b) the next merge wave recreates the gap.
- **Acceptance.** `/health` and `roomy.space/build.json` name commits that are ancestors of the ref the deploy watches, and that ref is stated in the repo; a merge to that ref is visible in a live build id within one deploy cycle.
- **Suggested priority:** **P1** — it gates the effect of every other item on this list.
- **Not already tracked because.** TASK-181 was the *image publication* half for the bridge (`#243`) and TASK-205/`#267` documented the bridge's publish path; nothing records which ref the **appserver** and **app-lite** deploy from, and this run's evidence (the app-lite stamp being on no fork ref at all) is new.
- **Honest limit.** I can prove the deployed commits and their ref relationships. I cannot see the platform's service configuration from here, so "the deploy watches `main`" is an **[INFERENCE]** from two consistent observations (the app-lite stamp is exactly `upstream/main`'s tip; the appserver build is `upstream/main~1` with one app-lite-only commit on top) rather than a read of a deploy config. Both halves agree, and the app-lite stamp is *only* explainable by a build from `upstream/main`.

### 2. The invites route still fires `getInvites` for callers the server can never allow — and `#271` fixes it on a ref that is not deployed

- **Evidence.** `GET /xrpc/space.roomy.space.getInvites failed: Member-created invites are disabled in this space` — **215 lines / 7d**, of which **46 fall on `c371a15c` in the 24h since its 10:31Z deploy**, newest 2026-09-25T18:xxZ. The 7d distribution by build is `908f3da2 65`, `46b515ec 24`, `824daab9 22`, `d32d84a5 42`, `e1aff94b 8`, `0f1b0b22 8`, `c371a15c 46` — so the rate did not fall after the last deploy; it is unchanged in kind.
  The mechanism is the one `#271` addresses: the handler 403s a non-admin when member invites are off (`handlers/space.roomy.space.getInvites.ts`), the sidebar *hides* the tab under that predicate, and the **route** fired the query unconditionally — reachable by URL for any member. `#271`'s own verification reports the control build issuing a 403 that re-fires on each re-entry, and zero requests with the change.
- **Why now.** The fix exists and is measured; it is on the wrong ref. Until **proposal 1** is resolved, every day of this signature is a day the built fix is not running. Restated as a live signal precisely because the count on the newest build (46 in 24h) is *not* a decline.
- **Scope sketch.** Deploy `#271` (proposal 1). No further code: `queries/invites.ts` and the page's `enabled` gate are in `#271`, and `git show origin/next:packages/app-lite/src/lib/queries/invites.ts` confirms `enabled` + `retryOnMount` are in tree.
- **Acceptance.** Zero `getInvites … disabled in this space` over 24h on a build that is an ancestor of the deployed ref, with an admin still loading the page.
- **Suggested priority:** **P2** — the request can never succeed, but it is a client-side re-issue of a 403, not a user-visible break.
- **Not already tracked because.** TASK-113/`#185` fixed the modal; `#271` (the route) closed as merged into `next` at 2026-09-25T13:57:59Z and **is not deployed**. No open entry connects the merged fix to the running build.

### 3. The embed sweeper still does not drain: 8,241 rows, `selectableRows 0`, and a 0.2% lifetime success rate, while `#270`'s fix sits undeployed

- **Evidence.** `/health/embed` at 20:2xZ: `pending 8241, inFlight 0, enrichedOk 90, enrichedNull 45108, transientBackoff 7513, backlogStuck true, backlogStuckSince 2026-09-25T20:12:34Z, lastStallCause "all-parked", lastCycle {pendingRows 8241, selectableRows 0, parkedRows 8241, backoffUrls 7513, selected 0}`. `/metrics` agrees: `roomy_embed_pending 8241`, `roomy_embed_selectable_rows 0`, `roomy_embed_backlog_stuck 1`. Lifetime success `90 / (90 + 45108)` = **0.20%**. `roomy_embed_pending` over the last 24h on a 3-hour step: `8185, 8192, 8200, 8204, 8206, 8202, 8213, 8235` — a slow climb, never a drain, across the whole window.
  The stall log is still the only thing `scope="embed-sweeper"` emits and still repeats: **605 lines / 24h, 365 / 12h, 203 / 6h** — i.e. ~0.8/min sustained, on the build that does not carry `#270`. `backlogStuckSkipped` was 115 at the sample, and the gauge `roomy_embed_backlog_stuck_transitions_total` that `#270` adds does **not** exist in `/metrics` (the `roomy_embed_*` set is exactly the nine series above).
- **Why now.** Two separate facts, and the fix addresses both: the backlog does not drain and the diagnostic that says so repeats ~600×/day for an unchanging cause. `#270` (merged 2026-09-25T13:58:46Z, `ff55d802`) implements both halves — `seedTransientRetry` restoring unexpired windows from `comp_embed_link_data` at boot, the latched `loggedStallCause`, the escalating stall poll, and `roomy_embed_backlog_stuck_transitions_total` — with its own smoke numbers (4 of 5 parked rows restored; 1 warn and 1 transition across 2,880 stalled cycles, versus baselines 2,081 and 396). It is merged and **undeployed**, so production is still on the old behaviour, and this is the same ref gap as proposal 1.
- **Scope sketch.** Deploy `#270` and re-measure `selectableRows` at the first cycle after the restart — that is the acceptance `#270` itself specifies, and it is the one measurement that cannot be taken while it is merged and not running. Then re-scope `#258` (**C6**) onto the deployed tree for its counters.
- **Acceptance.** After a restart carrying `#270`: `roomy_embed_selectable_rows` is materially below `pending` at the first cycle, `scope="embed-sweeper"` warn lines over 24h are in the low tens rather than ~600, and `roomy_embed_backlog_stuck_transitions_total` exists in a scrape.
- **Suggested priority:** **P2** — no user-visible break, but it is the difference between a measured fix and a fix that only exists in a branch.
- **Not already tracked because.** TASK-197/`#258` and TASK-201/`#270` are the *fixes*; nothing tracks the deploy of `#270` or re-measures the acceptance it states. This proposal is deliberately narrow: **not** a restatement of the embed defect, but the post-merge verification that cannot happen until it ships.
- **Honest limit.** I can measure the pre-deploy state completely and the merge completely. I have not seen `#270` run, and its smoke numbers are its author's, carried as their claim.

## Queue findings needing a decision

Audit results, not proposals: each is a queue-record question that needs Meri's word, and none is actionable by a worker. Re-verified this revision; where the state has changed since 09-24 I say so.

- **TASK-169 and TASK-188 are recorded `[done]` while `#234` and `#246` are open and unmerged, and neither fix is on any ref.** `GET /pulls/234` → `sorrel/task-169-queue-done-summary`, `state: open`, `merged_at: null`; `GET /pulls/246` → `sorrel/task-188-read-limit-cursor`, `state: open`, `merged_at: null`. Neither PR's head is contained by `next`: `git grep -n QueueJobSummary origin/next -- packages/` → no match; `git show origin/next:packages/cli/src/cli.ts` still declares `.option("--limit <n>", "Max messages", "20")` with no ceiling and no `--cursor`. The measured cost is unchanged: `Param limit must be ≤ 100` is **59 lines / 7d**, newest 2026-09-25T16:xxZ. Live confirmation for TASK-169 on this host: `~/.roomy/queue.json` is **1,028,158 B** with **41** `done[]` entries and `payload` still retained on every one of them — the exact shape `#234` removes, now larger than the 996,842 B / 33 entries measured yesterday. **Not closed here** (no unilateral closes, per the TASK-20 precedent) — flagged as dispatched.
- **TASK-21 ~ TASK-138 — duplicate, seventh consecutive flag.** Unchanged and still not dispatchable as written: TASK-21's title carries its own question mark and no acceptance criteria, and its outcome is a strict subset of TASK-138's. `#227` (TASK-138's plan) is `open`, `merged_at: null`, created 2026-09-16T15:30Z — 9 days old, the oldest PR in the queue. One word from Meri closes the pair; this needs a defer-or-decide rather than an eighth flag.
- **TASK-63 — blocked, seventh consecutive flag, 28 days at P1.** The plan (`docs/plans/voice-chat-plan.md`) is `Status: Draft for review`, dated 2026-08-28, and still awaits the same five decisions. New input only, not a re-argument: demand is now visible to users — Mr Blizzard asked in Muni Town `#roomy` at 2026-09-24T15:59Z *"Wait does roomy have voice chat?"*, answered "No, it doesn't yet" by Zicklag.
- **TASK-166(b) — recommend closing.** Its headline claim ("the SDK dist lags its own src") does not reproduce under a per-subpath test, and this run settles why: `stableKeyId` is declared in `packages/sdk/src/browser/tanstack.ts` and is **not** re-exported from `packages/sdk/src/browser/index.ts` (0 occurrences), so it is reachable through no subpath at all — `grep -o "function stableKeyId" packages/sdk/dist/browser/index.js` matches, and the built `dist/browser/index.js` names it in no `export{}` list. The original "absent from dist, present in src" comparison is true by construction, and `deleteMessages` (the claim's other half) *is* exported from root `dist/index.js`. Three scout runs have now spent effort re-testing an entry whose operational question ("fold an SDK rebuild into the next rollout?") has no measurement behind it. Recommend closing it outright rather than flagging it a fourth time.

## Log findings

- **`appserver` error volume, by day: 176 (09-18), 140 (09-19), 76 (09-20), 166 (09-21), 188 (09-22), 294 (09-23), 66 (09-24).** `level="error"` lines in the last 24h: **262**, and the dominant signatures are `Caller does not have write access to this room` **96**, `Authentication required` **65** (the server-side signature of TASK-19, which is open at P1), and `Member-created invites are disabled in this space` **46** (proposal 2), each followed by a long tail of 1–3. Counts here are instant `sum(count_over_time(…))` queries; a bounded range fetch truncates at its `limit` and undercounts these three by roughly half, which is how the previous revision of this file reported them.
- **`getMetadata failed` → 0 since the `c371a15c` deploy at 2026-09-25T10:31Z**, against 72 / 7d before it (all on builds ≤ `908f3da2`). TASK-198/`#257` is confirmed effective — a re-measurement, not a proposal.
- **`getSpaceSummary` 47 / 7d and `getRoomSummary` 28 / 7d**, of which 3 and 2 respectively fall after the `c371a15c` deploy, newest 2026-09-25T13:xxZ, with `watch` (19) and `p` (17) leading the space targets and `radial` (18) the room targets. Guards are in place at all three layers (markdown marking, prefetch, `retry: false` in `SpaceRoomBadge`), so this is the residue of a smaller, unidentified caller rather than the original defect — see C12. Not proposed: the counts are an order of magnitude below the proposal that produced the guards and every in-tree path is validated.
- **`Reply target … is not a message (no room)` — 22 lines / 7d, 10 of them on `c371a15c`.** This is the **write-side** guard (TASK-182) refusing a bad reply target at admission, and it is the healthy direction: the read-side twin (`Entity … is not a message`) is 244 / 7d with its newest at 2026-09-23T04:52:43Z, i.e. **0 in ~40h**. The write-side count means callers are still *attempting* room-targeted replies; they are now rejected rather than stored.
- **`Request timed out` — 12 lines / 7d, and the two on the current build are not a recurrence of TASK-155.** Both are 2026-09-25T13:15Z: `GET /xrpc/space.roomy.admin.listSpaces failed: Request timed out: query` and, 3 seconds later, `POST /xrpc/space.roomy.space.sendEvents failed: Request timed out: transaction`. `admin.listSpaces` receives ~2 requests/day (`increase(...[7d])` = 2.0) at p95 **28 s**, so this is the admin dashboard's own full-scan path (the endpoint TASK-116 optimised), not the discarded-membership read TASK-155 removed. `roomy_db_timeouts_total` still has **no series** in `/metrics` (fourth consecutive day), so an alert on it can never fire — worth a line in a metrics-hygiene task, not its own.
- **`Param limit must be ≤ 100` — 59 lines / 7d**, newest 2026-09-25T16:xxZ, values `200×16, 400×6, 300×3, 101×2, 150×2`. TASK-188/`#246` is open and clean and would close it; this is a **C5** item (the oldest-tail review backlog), not a new proposal.
- **discord-bridge: 971 lines in 24h**, of which `no capacity to enforce` is **24** (one per hour across 10 fail-open pairs — TASK-162's transition+summary form, working as designed) and the rest is ordinary `Connection error`/`code=1006` churn. The 7d `getPublicArchivedThreads failed` count is 5 and every one is `(discord status 403: Missing Access, code 50001)` on one channel, not the window defect C8 proposed.
- **`roomy_embed_*` in `/metrics` is exactly nine series**, and the two `#270` adds (`roomy_embed_backlog_stuck_transitions_total`) are absent; `roomy_embed_enriched_ok_total` / `…_definitive_total` / `…_transient_total` (the ones `#258` would add) are absent too. `roomy_embed_backlog_stuck_since_seconds` reads `1790367154`, an epoch-seconds *timestamp* under a name that reads as a duration — the HELP text is honest and `/health/embed` emits raw ms, so it is a naming trap rather than a bug, unchanged from the previous report.
- **`admin.getDashboardStats` is the slowest endpoint by p95: 28 s over 24h, on ~2 requests/day.** `search.messages` is next at 6.5 s (14 requests/day), `space.createSpace` 2.35 s (8/day). `space.getThreads` — the endpoint the 09-22 report escalated as a 7.45 s p95 — is now **0.155 s at p95 on 1,400 requests/day**, so TASK-194/`#252` and `#244` are confirmed effective on the deployed build. The admin endpoint is a single-digit-daily internal surface; recorded, not proposed.
- **Mimir otherwise healthy:** `roomy_process_starts_total` = 1 (no crash loop), `roomy_push_queued` = 0, `up{job="prometheus.scrape.appserver"}` present, all `roomy_pool_worker_pending` workers = 0.
- **`roomy-app-lite` telemetry is still dominated by one benign class** (`ResizeObserver loop completed with undelivered notifications`, ~866 / 7d, ~61–89% of the stream depending on the window), then `Script error.`, `Permission denied to access property`, push-registration failures, and stale-chunk dynamic-import failures. Unchanged in shape; rejected as noise for the fourth consecutive day.
- **Two findings from the 09-25 08:33Z tick that this document does not own.** The model retirement (`deepseek-v4-flash:0731`) and the hand-repair of two hosts' `~/.omp/agent/models.yml` are recorded in `#coordination` and are the *context* for **C4**; the code path they exposed is C4's subject. The five `TASK-192` scope PRs and today's seven new PRs are the coordinator's dispatches, not scout proposals.

## Considered and rejected

- **Re-litigating any carried verdict.** The carried items above are marked implemented (C7, C9, C10, C11, C12) or pending (C1–C6, C8-as-finding) on evidence re-measured this revision. Where a proposal's mechanism no longer exists in the tree (C8's `threadTimestampToCursor`), I say so and do not carry it.
- **The `getInvites` residual as a new *defect*.** It is **proposal 2** — a merged fix on an undeployed ref, which is proposal 1's gap. Filing it as a fresh defect would be the duplication TASK-142 warns about, on a handler whose fix already exists.
- **The `getMessage`/`Reply target` pair as a new defect.** Read-side 0 in ~40h (TASK-182's write guard holding), write-side 22/7d and expected (callers still attempt bad replies; the server refuses them). `#249` is open and clean for the remaining client guard. No new proposal.
- **`ResizeObserver loop completed with undelivered notifications`.** Fourth consecutive day of the same verdict: benign layout race, app renders in every probe, suppressing it in Faro would be masking.
- **`roomy_db_timeouts_total` never exported.** Fourth consecutive day. A metrics-hygiene line, not a task.
- **The `admin.listSpaces` p95 of 28 s.** Two requests/day on an internal admin surface, and TASK-116 already optimised this handler's full scan. A single 30 s timeout at 13:15Z today is the tail of a low-traffic endpoint, not a recurrence of TASK-155 (which was the discarded-membership read, and that is confirmed gone).
- **`roomy_cache` hit rate.** Unchanged reasoning: one process lifetime is too short a window to call a cache a win or a loss.
- **`~/.roomy/bin/` absent on this host** (sixth consecutive scout): `~/.roomy/` holds `queue.json`, `grafana.env`, `fleet-deploy.log`, `fleet-sdk-build`, `omp-sessions.json`. The CLI/Grafana/PR recipes were re-derived from the brief by hand again. TASK-141's gap, unchanged, and now also the reason the `#tasks`-room read window is a coordinator-side fix rather than a worker one.
- **The scout-PR process note itself.** Meri's 2026-09-25T12:54:39Z note is the reason this document exists in this form; it is not a proposal.

## Limits of the evidence

- **The two halves of this document were audited against different refs.** The proposals were read from `908f3da2` (`scout/2026-09-25`'s commit); the live-state, carried-forward and ref-gap sections were re-verified against production and the GitHub API at 20:3xZ, by which time `origin/next` had moved to `ff55d802`. Where a claim depends on current tree state (e.g. `#270`'s contents, `#271`'s `enabled` gates) I read `origin/next`, not the branch commit, and say which.
- **Proposal 1's deploy ref is an [INFERENCE] from two observations, not a read of a deploy config.** The app-lite build stamp is exactly `upstream/main`'s tip and the appserver build is `upstream/main~1` with one app-lite-only commit on top. No deploy configuration for either service is visible from this host.
- **C1's rollback cause is unmeasured.** I can show the ref moved forward and back inside 2.5 minutes (per the 09-24 push feed) and that the content is absent from every ref; I have no reviewer, comment, or CI failure naming a reason — the issue-events feed carries only `merged` then `closed`. "Deliberate revert" and "accident" remain equally consistent with what I can measure, which is precisely why re-landing is Meri's call.
- **C2 and proposal 3 are "merged and not running" claims, and they rest on the build id each process reports.** That is a direct measurement (every bridge line carries `d84e2a4d`; `/health` reports `c371a15c`), not an inspection of the service's image tag, which I cannot read from here.
- **Proposal 3 cannot yet be evaluated for effect**: `#270` is merged and undeployed, so its acceptance (a first cycle with `selectableRows` materially below `pending`) is untestable from a distance, and its smoke numbers are its author's.
- **Proposal 4 is latent by measurement, not by absence of code**: a 7-day query for the SDK's stop-signal strings returns 0 lines, and the probability argument is a code-path reading. The fix is merged; the exposure is the rollout.
- **`#bugs` has had no new message since 2026-09-18** and `#meta` none since 2026-09-11, so the community and self-improvement sources contributed no new *report* this run. The fresh coordination signal (the 08:33Z tick's model-retirement finding) is recorded as **C4**'s live instance, and the 16:17Z/20:16Z tick findings about the scout-PR rule are the process note this document implements.
- **`~/.roomy/bin/` is still absent on this host**, so the coordinator's `read-coord.ts` fix — which now scans `#tasks` as a secondary room — could not be read or re-verified from here; its behavior is reported in the task dispatch, not measured by me.
